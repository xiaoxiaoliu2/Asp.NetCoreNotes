@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@* used to specify the common layout view for multiple views in the same folder *@
@* specify this underscore layout as the default layout for all the viewers, so need not to mention the layout path in every file *@